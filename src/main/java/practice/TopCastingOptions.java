package practice;

import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chromium.ChromiumDriverCommandExecutor;
import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOptions {

	public static void main(String[] args) {
		SearchContext s = new RemoteWebDriver(null);
		WebDriver driver = new RemoteWebDriver(null);
		SearchContext s1 = new ChromeDriver();
		RemoteWebDriver r = new ChromeDriver();
		//searchcontext interface fe,fes
		//webdriver interface get,getTitle
		//remotewebdriver class
		//chromium driver
		//chromedriver
		//edge
		//firefox
		//safari
		//quit session id becomes null
		//close session id becomes invalid
		
		
		

	}

}
